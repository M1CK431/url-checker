model Website {
  host       String   @id // DB:["sqlite"]
  //host       String   @id @db.VarChar(260) // DB:["mysql","postgresql"]
  faviconUrl String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  deleted    Boolean? @default(false)

  reports Report[]
}
