model Website {
  host       String   @id // @db.VarChar(260)
  faviconUrl String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  deleted    Boolean? @default(false)

  reports Report[]
}
